return {
   bresenham_line =
       require("src.util.ds.point.bresenham_line"),

   chebyshev_distance =
       require("src.util.ds.point.chebyshev_distance"),

   get =
       require("src.util.ds.point.get"),

   euclidean_distance =
       require("src.util.ds.point.euclidean_distance"),

   is_neighbor =
       require("src.util.ds.point.is_neighbor"),

   is_next_to =
       require("src.util.ds.point.is_next_to"),

   manhattan_distance =
       require("src.util.ds.point.manhattan_distance"),

   offset =
       require("src.util.ds.point.offset"),

   overlaps =
       require("src.util.ds.point.overlaps"),
}
