return {
   new =
       require("game.util.ds.point.get"),

   bresenham_line =
       require("game.util.ds.point.bresenham_line"),

   chebyshev_distance =
       require("game.util.ds.point.chebyshev_distance"),

   euclidean_distance =
       require("game.util.ds.point.euclidean_distance"),

   is_neighbor =
       require("game.util.ds.point.is_neighbor"),

   is_next_to =
       require("game.util.ds.point.is_next_to"),

   manhattan_distance =
       require("game.util.ds.point.manhattan_distance"),

   offset =
       require("game.util.ds.point.offset"),

   overlaps =
       require("game.util.ds.point.overlaps"),
}
